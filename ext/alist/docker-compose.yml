version: '3.9'

services:
  alist:
    image: xhofe/alist:v2.6.4
    container_name: alist
    restart: unless-stopped
    network_mode: host
    environment:
      TZ: Asia/Shanghai
    volumes:
      - ./alist-data:/opt/alist/data
      - /mnt/user/datas/alist:/mnt/data
